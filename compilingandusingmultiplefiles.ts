// first make tscofig file by typing tsc -init in terminal

// then type tsc -w for converting all unconverted ts files to js

// when the tsconfig file is created you can use include or exclude to  make an array for those files and folder to either be converted to js or not do  add these at the end

// when compiled es5 changes let and const to var but if we use es6 or above it wont to change it go to tsconfig file then find target and change it to es6
