
// array types

let arr = ['sds',12,true] //if nothing is assigned then it takes the normal value

let aww:any =['sds',12,true] //can be assigned for any value

let aqq:string[]= ['dsfsdd','dfds']  //can be used to say which type should  be inside

let ass:[string,number,boolean]=['sds',12,true]  // method only used when length and types are known this also tuple

let azzz:string[] | number[]=[32]; // or operator it either be string or number
